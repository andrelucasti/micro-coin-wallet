spring:
  application:
    name: wallet-manager
  profiles:
    active: dev
server:
  port: 8929

management:
  endpoints:
    web:
      exposure:
        include: '*'


integration:
  portfolio:
    topic: wallet-manager-portfolio
